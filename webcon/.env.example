# Static token required to open the UI / WebSocket. Set to something long and random.
WEBCON_TOKEN=change-me-to-a-long-random-string

# RCON password — must match CS2_RCONPW in the gameserver compose env.
RCON_PASSWORD=CHANGE_ME_RCON
